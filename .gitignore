node_modules
dist
.DS_Store
**/.DS_Store

# Environment / secrets — never commit any of these
.env
.env.*
*.local

# SSL / private keys
*.pem
*.key
*.cert
*.p12

# Local Netlify folder
.netlify

# Supabase CLI temp files — contain project ref and connection metadata.
# Not secrets, but no reason to track them in git.
supabase/.temp/
